apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: html-pvc
  labels:
    app: html
    environment: dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
